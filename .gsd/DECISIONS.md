# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Migration direction | One-way .planning → .gsd only | Two-way sync adds complexity without value; migration is a one-time upgrade | No |
| D002 | M001 | arch | Structural mapping | Phases → Slices, Plans → Tasks | Natural hierarchy mapping; phase = demoable unit = slice, plan = atomic work = task | No |
| D003 | M001 | convention | CLI surface | /gsd migrate subcommand | Consistent with existing /gsd command routing; discoverable within the tool | No |
| D004 | M001 | scope | Migration target | Single .planning directory per invocation | Simpler, safer, more predictable than batch scanning; batch deferred to R010 | Yes — if users request batch |
| D005 | M001 | convention | Preview before write | Required confirmation step | Irreversible directory write needs user confidence; show file counts and structure | No |
| D006 | M001 | arch | Completion state | Preserve from source | Users migrating active projects need continuity; completed phases → [x] slices | No |
| D007 | M001 | arch | Research migration | Consolidate into milestone research | Preserves expensive agent work; project-level research → M001-RESEARCH.md, per-phase → slice research | No |
| D008 | M001 | convention | Module location | src/resources/extensions/gsd/migrate/ | Keeps migration code isolated from core GSD logic; easy to find and maintain | Yes — if it should be a separate extension |
| D009 | M001/S01 | arch | Export files.ts helpers | Add export to 6 private helpers in files.ts | Parser needs identical markdown utilities; re-implementing would duplicate tested code. Export-only change, no signature changes. | No |
| D010 | M001/S01 | arch | Fixture-based parser tests | Synthetic .planning dirs via mkdtempSync, not real project dirs | Reproducible, fast, no external dependency; covers all edge cases from research. Matches derive-state.test.ts pattern. | No |
| D011 | M001/S01 | convention | Phase key = full directory name | Use "45-logging-config-standardization" not just "45" | Handles duplicate phase numbers across milestones (discovered in nexus). Number alone is not unique. | No |
| D012 | M001/S01 | arch | Hyphenated-key YAML parser in parsers.ts | Local `parseFrontmatterMapHyphen()` instead of modifying `files.ts` `parseFrontmatterMap` | Old .planning summaries use hyphenated keys (tech-stack, key-files, key-decisions, patterns-established). Existing parseFrontmatterMap uses `\w` regex that excludes hyphens. Modifying the shared helper risks regressions in 249 existing parser tests. Local copy in migrate/parsers.ts is safer and scoped to migration. | Yes — could upstream if tests confirm safety |
| D013 | 2026-03-11 | M001 | Old phase numbers not preserved in slice IDs | Sequential S01, S02... renumbering after float-sort | Old phase numbers (e.g. 45, 46, 2.1) are artifacts of insertion order, not meaningful identifiers. Sequential IDs are cleaner and match GSD-2 conventions. Original phase names preserved in slice titles. | Yes — could add `originalPhase` metadata if needed |
| D014 | 2026-03-11 | M001 | Four-stage pipeline architecture (parse → transform → write → command) | Decoupled stages with typed boundaries, each independently testable | Each stage has a single responsibility: parser owns I/O + format knowledge, transformer owns business logic + field mapping, writer owns serialization, command owns UX. Pure functions at each boundary enable fixture-based testing without filesystem or UI dependencies. | No — fundamental to the module structure |
| D015 | 2026-03-11 | M001 | Transformer output types mirror GSD-2 parser input types | Design `GSDProject`/`GSDSlice`/`GSDTask` to match shapes that `parseRoadmap()`, `parsePlan()`, `parseSummary()` read | Ensures writer is purely mechanical serialization and `deriveState()` works on migrated output without special cases. The transformer is the translation layer; the writer just renders. | No — core design invariant |
| D016 | 2026-03-11 | M001 | Roadmap checkbox is source of truth for slice completion, summary existence for task completion | Trust `[x]` from old ROADMAP.md for slice done state; trust summary file existence for task done state | These two signals can disagree (phase marked done but missing task summaries). Roadmap is the authoritative record for phase-level completion in the old system. | No — matches old system semantics |
| D017 | 2026-03-11 | M001 | No REQUIREMENTS.md file — requirement tracking via M001-CONTEXT.md only | Legacy compatibility mode for requirement coverage | R001–R009 defined in context document; formal requirements file not needed for a single-milestone tool project. Coverage tracked in roadmap. | Yes — add REQUIREMENTS.md if project grows |
| D018 | 2026-03-11 | S02 | Transformer tests use in-memory fixtures, not filesystem | Build synthetic `PlanningProject` objects directly in test code | Transformer is pure — no I/O. In-memory fixtures are faster, simpler, and test exactly the transform contract without coupling to parser behavior. | No — follows S01 pattern and D002 |
| D019 | 2026-03-11 | S02 | Boundary map defaults to empty array in migrated output | `GSDMilestone.boundaryMap = []` for all migrated milestones | Old format has no boundary map equivalent. Empty array is valid for `deriveState()` — `parseRoadmap()` returns empty boundary map when section is missing. | Yes — could generate stub entries from phase dependencies |
| D020 | 2026-03-11 | S02 | Research consolidation uses fixed file-type ordering | SUMMARY → ARCHITECTURE → STACK → FEATURES → PITFALLS → alphabetical remainder | Consistent ordering makes consolidated research predictable and readable. Mirrors typical consumption order. | Yes — ordering is cosmetic |
| D021 | 2026-03-11 | S02 | Vision derivation uses a three-level fallback chain | First sentence of first non-heading PROJECT.md line → first roadmap milestone title → static default | Gracefully handles missing or heading-only PROJECT.md without crashing. First prose line is the most human-readable summary of what a project is. | Yes — could extract from description field if one is added |
| D022 | 2026-03-11 | S02 | Requirements auto-ID uses sequential counter across all requirements | R001, R002, ... assigned in order encountered across all requirements in project | Existing IDs are preserved as-is. Auto-generated IDs are collision-free within a single migration run. Counter is global (not per-milestone) for simplicity — migration is a one-shot operation. | No |
| D023 | 2026-03-11 | S02 | Unknown requirement statuses normalize to 'active' | normalizeStatus() maps any unrecognized value to 'active' | Old format had no strict status enum. Silent normalization is safer than crashing or emitting invalid status strings. 'active' is the least-surprising default for unmapped work. | No |
| D024 | 2026-03-11 | S03 | serializeFrontmatter implemented locally in writer.ts | Local implementation, not exported from files.ts | files.ts has a private formatFrontmatter helper with incompatible signature. Exporting it would change the public API of a shared module for a migration-only use case. Local copy in writer.ts is scoped, clear, and doesn't risk regressions in the core parser. | Yes — could upstream if a general YAML frontmatter serializer is needed elsewhere |
| D025 | 2026-03-11 | S03 | writeGSDDirectory skips null research and null summary fields | No empty stub files written for null fields | Empty stub files add noise to the written directory and produce empty sections when parsed. Skipping them keeps the output clean and matches how real GSD-2 projects work — research and summaries are only present when they have content. | No |
