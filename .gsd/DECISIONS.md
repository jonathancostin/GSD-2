# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | 2026-03-11 | M001 | Old phase numbers not preserved in slice IDs | Sequential S01, S02... renumbering after float-sort | Old phase numbers (e.g. 45, 46, 2.1) are artifacts of insertion order, not meaningful identifiers. Sequential IDs are cleaner and match GSD-2 conventions. Original phase names preserved in slice titles. | Yes — could add `originalPhase` metadata if needed |
| D002 | 2026-03-11 | M001 | Four-stage pipeline architecture (parse → transform → write → command) | Decoupled stages with typed boundaries, each independently testable | Each stage has a single responsibility: parser owns I/O + format knowledge, transformer owns business logic + field mapping, writer owns serialization, command owns UX. Pure functions at each boundary enable fixture-based testing without filesystem or UI dependencies. | No — fundamental to the module structure |
| D003 | 2026-03-11 | M001 | Transformer output types mirror GSD-2 parser input types | Design `GSDProject`/`GSDSlice`/`GSDTask` to match shapes that `parseRoadmap()`, `parsePlan()`, `parseSummary()` read | Ensures writer is purely mechanical serialization and `deriveState()` works on migrated output without special cases. The transformer is the translation layer; the writer just renders. | No — core design invariant |
| D004 | 2026-03-11 | M001 | Roadmap checkbox is source of truth for slice completion, summary existence for task completion | Trust `[x]` from old ROADMAP.md for slice done state; trust summary file existence for task done state | These two signals can disagree (phase marked done but missing task summaries). Roadmap is the authoritative record for phase-level completion in the old system. | No — matches old system semantics |
| D005 | 2026-03-11 | M001 | No REQUIREMENTS.md file — requirement tracking via M001-CONTEXT.md only | Legacy compatibility mode for requirement coverage | R001–R009 defined in context document; formal requirements file not needed for a single-milestone tool project. Coverage tracked in roadmap. | Yes — add REQUIREMENTS.md if project grows |
| D006 | 2026-03-11 | S02 | Transformer tests use in-memory fixtures, not filesystem | Build synthetic `PlanningProject` objects directly in test code | Transformer is pure — no I/O. In-memory fixtures are faster, simpler, and test exactly the transform contract without coupling to parser behavior. | No — follows S01 pattern and D002 |
| D007 | 2026-03-11 | S02 | Boundary map defaults to empty array in migrated output | `GSDMilestone.boundaryMap = []` for all migrated milestones | Old format has no boundary map equivalent. Empty array is valid for `deriveState()` — `parseRoadmap()` returns empty boundary map when section is missing. | Yes — could generate stub entries from phase dependencies |
| D008 | 2026-03-11 | S02 | Research consolidation uses fixed file-type ordering | SUMMARY → ARCHITECTURE → STACK → FEATURES → PITFALLS → alphabetical remainder | Consistent ordering makes consolidated research predictable and readable. Mirrors typical consumption order. | Yes — ordering is cosmetic |
